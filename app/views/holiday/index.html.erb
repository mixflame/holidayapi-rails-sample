<strong>Holiday API Sample</strong>
<p>Delivers results from <a href="https://holidayapi.com">HolidayApi</a>!</p>

<a href="/languages">Get list of languages and codes</a>

<p>
<h2>Get 2019 Holidays for a Country (2 letter code)</h2>
<%= form_tag( '/holidays', method: :get) do %>
    <p>
        <%= label_tag ("country") do %>
            Country: <%= select_tag "country", options_for_select(@countries.map { |obj| [obj['name'], obj['code']] }) %>
        <% end %>
    </p>
    <p>
        <%= submit_tag 'Get Holidays' %>
    </p>
<% end %>
</p>

<p>
<h2>Calculate the workday that occurs a given number of days after a date</h2>
<%= form_tag( '/workday', method: :get) do %>

    <p>
    <%= label_tag ("country") do %>
        Country: <%= select_tag "country", options_for_select(@countries.map { |obj| [obj['name'], obj['code']] }) %>
    <% end %>
    </p>
    <p>
    <%= label_tag ("start") do %>
        Start (YYYY-MM-DD): <%= text_field_tag ("start") %>
    <% end %>
    </p>
    <p>
    <%= label_tag ("days") do %>
        Number of days to advanced from start (positive integer): <%= text_field_tag ("days") %>
    <% end %>
    </p>
    <p>
    <%= submit_tag 'Get Workday' %>
    </p>
<% end %>
</p>